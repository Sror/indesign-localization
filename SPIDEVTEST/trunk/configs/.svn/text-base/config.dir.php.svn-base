<?php
//URL Path Setup
define("LOGO_PATH","uploads/logos/");								//logo upload path
define("PHOTO_PATH","uploads/photos/");								//photo upload path
define("FTP_DIR","ftp/");											//ftp directory
define("TMP_DIR","tmp/");											//tmp path
define("DOC_POOL","docpool/");										//docpool path KEEP TRAILING SLASH
define("PREVIEW_DIR","docpool/previews/");							//preview jpegs path KEEP TRAILING SLASH
define("POSTVIEW_DIR","docpool/postviews/");						//postview jpegs path KEEP TRAILING SLASH
define("TEXTBOXES_DIR","textboxes/");								//textbox previews path KEEP TRAILING SLASH
define("THUMBNAILS_DIR","thumbnails/");								//thumbnails dir KEEP TRAILING SLASH
define("EDITS_DIR","edits/");										//edits dir KEEP TRAILING SLASH

define("JOBOPTIONS", "PDF_Profile");
define("JOBOPTIONS_DIR",ROOT."PDF_Profile/");

//Folders Setup
define("LANG", ROOT."languages/");
define("MODULES", ROOT."modules/");
define("CLASSES", ROOT."classes/");
define("ENGINES", CLASSES."engines/");
define("PROCESSES", CLASSES."processes/");
define("RESOURCES", ROOT."resources/");
define("ACL", ROOT."acl/");

//create folders if not existed
if(!file_exists(ROOT.'uploads')) mkdir(ROOT.'uploads');			
if(!file_exists(ROOT.LOGO_PATH)) mkdir(ROOT.LOGO_PATH);											//logo folder
if(!file_exists(ROOT.PHOTO_PATH)) mkdir(ROOT.PHOTO_PATH);										//photo folder
if(!file_exists(ROOT.FTP_DIR)) mkdir(ROOT.FTP_DIR);												//ftp folder
if(!file_exists(ROOT.TMP_DIR)) mkdir(ROOT.TMP_DIR);												//tmp folder
if(!file_exists(ROOT.DOC_POOL)) mkdir(ROOT.DOC_POOL);											//preview docpool
if(!file_exists(ROOT.PREVIEW_DIR)) mkdir(ROOT.PREVIEW_DIR);										//preview jpgs folder
if(!file_exists(ROOT.POSTVIEW_DIR)) mkdir(ROOT.POSTVIEW_DIR);									//postview jpgs folder
if(!file_exists(ROOT.PREVIEW_DIR.TEXTBOXES_DIR)) mkdir(ROOT.PREVIEW_DIR.TEXTBOXES_DIR);			//textbox preview folder
if(!file_exists(ROOT.POSTVIEW_DIR.TEXTBOXES_DIR)) mkdir(ROOT.POSTVIEW_DIR.TEXTBOXES_DIR);		//textbox postview folder
if(!file_exists(ROOT.PREVIEW_DIR.THUMBNAILS_DIR)) mkdir(ROOT.PREVIEW_DIR.THUMBNAILS_DIR);		//thumbnails preview folder
if(!file_exists(ROOT.POSTVIEW_DIR.THUMBNAILS_DIR)) mkdir(ROOT.POSTVIEW_DIR.THUMBNAILS_DIR);		//thumbnails postview folder
if(!file_exists(ROOT.PREVIEW_DIR.EDITS_DIR)) mkdir(ROOT.PREVIEW_DIR.EDITS_DIR);					//edits postview folder
if(!file_exists(ROOT.POSTVIEW_DIR.EDITS_DIR)) mkdir(ROOT.POSTVIEW_DIR.EDITS_DIR);				//edits postview folder
if(!file_exists(UPLOAD_DIR)) mkdir(UPLOAD_DIR);													//upload folder
if(!file_exists(IMG_LIBRARY_DIR)) mkdir(IMG_LIBRARY_DIR);										//image library
if(!file_exists(REPOSITORY_DIR)) mkdir(REPOSITORY_DIR);											//file repository
if(!file_exists(OUTPUT_DIR)) mkdir(OUTPUT_DIR);													//output folder
if(!file_exists(JOBOPTIONS_DIR)) mkdir(JOBOPTIONS_DIR);													//output folder